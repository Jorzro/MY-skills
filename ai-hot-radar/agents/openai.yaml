interface:
  display_name: "AI Hot Radar"
  short_description: "AI资讯中文简报、首次问卷、评分、心跳和多API海报图"
  brand_color: "#0F766E"
  default_prompt: "Use $ai-hot-radar. If first run, ask the 5-question Chinese onboarding survey first; otherwise summarize today's AI news as text or generate a poster image based on my saved preference."

policy:
  allow_implicit_invocation: true
